<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SPECTRA-7 // Anomaly at 656nm</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Space+Mono:ital,wght@0,400;0,700;1,400&family=Inter:wght@300;400;500;600&display=swap');

  :root {
    --void: #05060a;
    --deep: #0b0e18;
    --panel: #111420;
    --border: #1e2235;
    --h-alpha: #e8405a;
    --h-alpha-glow: rgba(232, 64, 90, 0.25);
    --anomaly: #ff6b35;
    --anomaly-glow: rgba(255, 107, 53, 0.3);
    --cold: #4fc3f7;
    --cold-dim: rgba(79, 195, 247, 0.15);
    --text: #c8cfe0;
    --text-dim: #5a6080;
    --text-bright: #e8ecf8;
    --green: #4caf87;
    --amber: #f5a623;
    --mono: 'Space Mono', monospace;
    --body: 'Inter', sans-serif;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: var(--void);
    color: var(--text);
    font-family: var(--body);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* STARFIELD */
  .starfield {
    position: fixed; inset: 0; pointer-events: none; z-index: 0;
    background: 
      radial-gradient(1px 1px at 15% 20%, rgba(255,255,255,0.6) 0%, transparent 100%),
      radial-gradient(1px 1px at 72% 8%, rgba(255,255,255,0.4) 0%, transparent 100%),
      radial-gradient(1px 1px at 38% 55%, rgba(255,255,255,0.5) 0%, transparent 100%),
      radial-gradient(1px 1px at 91% 33%, rgba(255,255,255,0.3) 0%, transparent 100%),
      radial-gradient(1px 1px at 55% 78%, rgba(255,255,255,0.4) 0%, transparent 100%),
      radial-gradient(1px 1px at 28% 90%, rgba(255,255,255,0.3) 0%, transparent 100%),
      radial-gradient(1px 1px at 84% 65%, rgba(255,255,255,0.5) 0%, transparent 100%),
      radial-gradient(1px 1px at 6% 45%, rgba(255,255,255,0.4) 0%, transparent 100%),
      var(--void);
  }

  .container {
    position: relative; z-index: 1;
    max-width: 900px;
    margin: 0 auto;
    padding: 40px 20px 80px;
  }

  /* HEADER */
  .alert-bar {
    display: flex; align-items: center; gap: 12px;
    background: rgba(232, 64, 90, 0.08);
    border: 1px solid rgba(232, 64, 90, 0.3);
    border-radius: 4px;
    padding: 10px 16px;
    margin-bottom: 32px;
    font-family: var(--mono);
    font-size: 11px;
    color: var(--h-alpha);
    letter-spacing: 0.1em;
  }

  .alert-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--h-alpha);
    animation: pulse 1.4s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.7); }
  }

  .title-block { margin-bottom: 40px; }

  .eyebrow {
    font-family: var(--mono);
    font-size: 10px;
    letter-spacing: 0.2em;
    color: var(--text-dim);
    text-transform: uppercase;
    margin-bottom: 12px;
  }

  h1 {
    font-family: var(--mono);
    font-size: clamp(22px, 4vw, 32px);
    font-weight: 700;
    color: var(--text-bright);
    line-height: 1.2;
    margin-bottom: 10px;
  }

  h1 span {
    color: var(--h-alpha);
  }

  .subtitle {
    font-size: 14px;
    color: var(--text-dim);
    line-height: 1.6;
    max-width: 560px;
  }

  /* SPECTROGRAPH — the signature element */
  .spectrograph-wrap {
    margin: 36px 0;
    background: var(--deep);
    border: 1px solid var(--border);
    border-radius: 6px;
    overflow: hidden;
  }

  .spec-header {
    display: flex; justify-content: space-between; align-items: center;
    padding: 12px 16px;
    border-bottom: 1px solid var(--border);
    font-family: var(--mono);
    font-size: 10px;
    color: var(--text-dim);
    letter-spacing: 0.08em;
  }

  .spec-canvas {
    padding: 24px 16px 12px;
    position: relative;
  }

  /* Continuous spectrum bar */
  .spectrum-bar {
    height: 40px;
    border-radius: 3px;
    background: linear-gradient(to right,
      #3a006f 0%,       /* 380nm */
      #5500cc 8%,
      #0033ff 20%,      /* 430nm */
      #0099ff 32%,
      #00cccc 42%,
      #00ff88 52%,      /* 530nm */
      #aaff00 60%,
      #ffee00 68%,
      #ff8800 78%,
      #ff2200 88%,      /* 660nm */
      #880000 95%,
      #330000 100%       /* 700nm */
    );
    position: relative;
    margin-bottom: 20px;
  }

  /* Expected H-alpha line */
  .line-expected {
    position: absolute;
    top: -6px; bottom: -6px;
    width: 2px;
    background: rgba(255,255,255,0.5);
    left: calc(82% - 1px); /* 656nm position */
  }

  .line-expected::before {
    content: 'Hα 656.3nm';
    position: absolute;
    top: -22px; left: 50%;
    transform: translateX(-50%);
    font-family: var(--mono);
    font-size: 9px;
    color: rgba(255,255,255,0.4);
    white-space: nowrap;
  }

  /* Anomalous emission — slightly offset, wrong profile */
  .line-anomaly {
    position: absolute;
    top: -10px; bottom: -10px;
    width: 6px;
    background: linear-gradient(to right, transparent, var(--anomaly) 50%, transparent);
    left: calc(82.5% - 3px);
    filter: blur(1px);
    box-shadow: 0 0 12px var(--anomaly-glow), 0 0 24px var(--anomaly-glow);
  }

  .line-anomaly::after {
    content: '⚠ ANOMALY';
    position: absolute;
    bottom: -22px; left: 50%;
    transform: translateX(-50%);
    font-family: var(--mono);
    font-size: 9px;
    color: var(--anomaly);
    white-space: nowrap;
    letter-spacing: 0.1em;
  }

  /* Wavelength axis */
  .wl-axis {
    display: flex; justify-content: space-between;
    padding: 0 0 4px;
    font-family: var(--mono);
    font-size: 9px;
    color: var(--text-dim);
    margin-top: 28px;
  }

  /* Emission profile chart */
  .profile-wrap {
    padding: 0 16px 20px;
    border-top: 1px solid var(--border);
  }

  .profile-label {
    font-family: var(--mono);
    font-size: 9px;
    color: var(--text-dim);
    letter-spacing: 0.08em;
    padding: 12px 0 8px;
  }

  .profile-svg { width: 100%; overflow: visible; }

  /* GRID */
  .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 12px; }
  .grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 12px; margin-bottom: 12px; }

  @media (max-width: 600px) {
    .grid-2, .grid-3 { grid-template-columns: 1fr; }
  }

  /* DATA PANELS */
  .panel {
    background: var(--panel);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 16px;
  }

  .panel-label {
    font-family: var(--mono);
    font-size: 9px;
    letter-spacing: 0.15em;
    color: var(--text-dim);
    text-transform: uppercase;
    margin-bottom: 10px;
  }

  .data-row {
    display: flex; justify-content: space-between; align-items: baseline;
    padding: 5px 0;
    border-bottom: 1px solid rgba(30, 34, 53, 0.6);
    font-size: 12px;
  }

  .data-row:last-child { border-bottom: none; }
  .data-key { color: var(--text-dim); font-family: var(--mono); font-size: 10px; }
  .data-val { color: var(--text-bright); font-family: var(--mono); font-size: 11px; }
  .data-val.flag { color: var(--anomaly); }
  .data-val.ok { color: var(--green); }
  .data-val.warn { color: var(--amber); }

  /* SECTION HEADINGS */
  .section-title {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.15em;
    color: var(--text-dim);
    text-transform: uppercase;
    margin: 32px 0 14px;
    display: flex; align-items: center; gap: 12px;
  }

  .section-title::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--border);
  }

  /* HYPOTHESIS CARDS */
  .hyp-card {
    background: var(--panel);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 16px;
    position: relative;
    transition: border-color 0.2s;
    cursor: pointer;
    margin-bottom: 10px;
  }

  .hyp-card:hover { border-color: var(--cold); }
  .hyp-card.selected { border-color: var(--anomaly); background: rgba(255, 107, 53, 0.05); }

  .hyp-id {
    font-family: var(--mono);
    font-size: 9px;
    color: var(--text-dim);
    margin-bottom: 6px;
    letter-spacing: 0.1em;
  }

  .hyp-name {
    font-size: 13px;
    font-weight: 600;
    color: var(--text-bright);
    margin-bottom: 6px;
  }

  .hyp-desc {
    font-size: 11px;
    color: var(--text-dim);
    line-height: 1.6;
  }

  .hyp-prob {
    position: absolute;
    top: 14px; right: 14px;
    font-family: var(--mono);
    font-size: 11px;
    color: var(--cold);
  }

  .prob-bar-wrap {
    height: 3px;
    background: var(--border);
    border-radius: 2px;
    margin-top: 10px;
    overflow: hidden;
  }

  .prob-bar {
    height: 100%;
    border-radius: 2px;
    background: var(--cold);
    transition: width 0.5s ease;
  }

  /* INVESTIGATE BUTTON */
  .investigate-btn {
    display: block;
    width: 100%;
    margin-top: 24px;
    padding: 14px;
    background: transparent;
    border: 1px solid var(--h-alpha);
    border-radius: 4px;
    color: var(--h-alpha);
    font-family: var(--mono);
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background 0.2s, color 0.2s;
  }

  .investigate-btn:hover {
    background: var(--h-alpha);
    color: var(--void);
  }

  .investigate-btn:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  /* REASONING PANEL */
  .reasoning-panel {
    display: none;
    margin-top: 24px;
    background: var(--deep);
    border: 1px solid var(--border);
    border-radius: 6px;
    overflow: hidden;
  }

  .reasoning-panel.active { display: block; }

  .reasoning-header {
    padding: 12px 16px;
    border-bottom: 1px solid var(--border);
    font-family: var(--mono);
    font-size: 10px;
    color: var(--cold);
    letter-spacing: 0.1em;
    display: flex; align-items: center; gap: 8px;
  }

  .reasoning-body {
    padding: 20px;
    font-size: 13px;
    line-height: 1.8;
    color: var(--text);
  }

  .reasoning-body .key-term {
    color: var(--anomaly);
    font-family: var(--mono);
    font-size: 11px;
  }

  .loading-dots {
    display: inline-flex; gap: 4px; align-items: center;
  }

  .loading-dots span {
    width: 4px; height: 4px;
    border-radius: 50%;
    background: var(--cold);
    animation: dot-blink 1.2s ease-in-out infinite;
  }

  .loading-dots span:nth-child(2) { animation-delay: 0.2s; }
  .loading-dots span:nth-child(3) { animation-delay: 0.4s; }

  @keyframes dot-blink {
    0%, 80%, 100% { opacity: 0.2; }
    40% { opacity: 1; }
  }

  /* VERDICT BLOCK */
  .verdict-block {
    display: none;
    margin-top: 16px;
    padding: 16px;
    border-radius: 4px;
    border-left: 3px solid var(--green);
    background: rgba(76, 175, 135, 0.06);
    font-size: 12px;
    line-height: 1.7;
    color: var(--text);
  }

  .verdict-block.active { display: block; }

  .verdict-title {
    font-family: var(--mono);
    font-size: 10px;
    color: var(--green);
    letter-spacing: 0.12em;
    margin-bottom: 8px;
  }

  /* OBSERVATION LOG */
  .obs-entry {
    display: flex; gap: 12px;
    padding: 8px 0;
    border-bottom: 1px solid rgba(30, 34, 53, 0.5);
    font-family: var(--mono);
    font-size: 10px;
  }

  .obs-ts { color: var(--text-dim); flex-shrink: 0; }
  .obs-msg { color: var(--text); }
  .obs-msg.alert { color: var(--h-alpha); }
  .obs-msg.info { color: var(--cold); }

  /* SCROLLBAR */
  ::-webkit-scrollbar { width: 4px; }
  ::-webkit-scrollbar-track { background: var(--deep); }
  ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 2px; }
</style>
</head>
<body>

<div class="starfield"></div>

<div class="container">

  <!-- ALERT BAR -->
  <div class="alert-bar">
    <div class="alert-dot"></div>
    SPECTRA-7 // ANOMALY CLASSIFICATION PROTOCOL — ACTIVE OBSERVATION SESSION
  </div>

  <!-- TITLE -->
  <div class="title-block">
    <div class="eyebrow">Spectroscopic Anomaly // Case ID: SA-2026-0041</div>
    <h1>Emission at <span>656nm</span><br>Does Not Resolve as Hα</h1>
    <p class="subtitle">A spectral line has been detected at the wavelength of hydrogen's Balmer alpha transition — but its profile, intensity ratio, and temporal behaviour are inconsistent with standard recombination emission. Diagnose the source.</p>
  </div>

  <!-- SPECTROGRAPH -->
  <div class="spectrograph-wrap">
    <div class="spec-header">
      <span>DETECTOR: OPTICAL // RES: 0.1Å/px // RANGE: 380–700nm</span>
      <span style="color: var(--anomaly);">⚠ PROFILE MISMATCH DETECTED</span>
    </div>
    <div class="spec-canvas">
      <div class="spectrum-bar">
        <div class="line-expected"></div>
        <div class="line-anomaly"></div>
      </div>
      <div class="wl-axis">
        <span>380nm</span>
        <span>430nm</span>
        <span>480nm</span>
        <span>530nm</span>
        <span>580nm</span>
        <span>630nm</span>
        <span>680nm</span>
        <span>700nm</span>
      </div>
    </div>

    <!-- EMISSION PROFILE -->
    <div class="profile-wrap">
      <div class="profile-label">EMISSION LINE PROFILE — OBSERVED vs EXPECTED (normalised flux)</div>
      <svg class="profile-svg" viewBox="0 0 860 120" preserveAspectRatio="none" height="100">
        <!-- Grid lines -->
        <line x1="0" y1="100" x2="860" y2="100" stroke="#1e2235" stroke-width="1"/>
        <line x1="0" y1="70" x2="860" y2="70" stroke="#1e2235" stroke-width="0.5" stroke-dasharray="3,4"/>
        <line x1="0" y1="40" x2="860" y2="40" stroke="#1e2235" stroke-width="0.5" stroke-dasharray="3,4"/>
        <line x1="0" y1="10" x2="860" y2="10" stroke="#1e2235" stroke-width="0.5" stroke-dasharray="3,4"/>

        <!-- Expected narrow Gaussian (normal Hα) -->
        <path d="M 680,100 Q 690,100 695,95 Q 700,60 705,10 Q 710,60 715,95 Q 720,100 730,100" 
              fill="none" stroke="rgba(255,255,255,0.3)" stroke-width="1.5" stroke-dasharray="4,3"/>

        <!-- Observed — broader, slightly redshifted, asymmetric (P Cygni-like shoulder) -->
        <path d="M 660,100 Q 668,100 675,97 Q 685,80 695,40 Q 700,15 710,8 Q 720,15 728,35 Q 738,65 748,90 Q 758,100 770,100"
              fill="rgba(255, 107, 53, 0.12)" stroke="var(--anomaly)" stroke-width="2"/>

        <!-- Blue absorption trough (P Cygni signature) -->
        <path d="M 620,100 Q 628,100 635,98 Q 642,88 648,102 Q 652,110 660,100"
              fill="rgba(79, 195, 247, 0.08)" stroke="var(--cold)" stroke-width="1.5"/>

        <!-- Labels -->
        <text x="697" y="24" fill="rgba(255,255,255,0.3)" font-size="9" font-family="Space Mono" text-anchor="middle">Expected</text>
        <text x="740" y="30" fill="#ff6b35" font-size="9" font-family="Space Mono">Observed</text>
        <text x="636" y="84" fill="#4fc3f7" font-size="9" font-family="Space Mono" text-anchor="middle">Abs.</text>
      </svg>
    </div>
  </div>

  <!-- OBSERVATIONAL DATA -->
  <div class="section-title">Observational Parameters</div>
  <div class="grid-3">
    <div class="panel">
      <div class="panel-label">Line Properties</div>
      <div class="data-row"><span class="data-key">Peak λ</span><span class="data-val flag">656.8nm</span></div>
      <div class="data-row"><span class="data-key">Hα rest λ</span><span class="data-val">656.28nm</span></div>
      <div class="data-row"><span class="data-key">FWHM</span><span class="data-val flag">~180 km/s</span></div>
      <div class="data-row"><span class="data-key">Expected FWHM</span><span class="data-val ok">~20 km/s</span></div>
      <div class="data-row"><span class="data-key">Profile</span><span class="data-val flag">Asymmetric</span></div>
    </div>
    <div class="panel">
      <div class="panel-label">Intensity Ratios</div>
      <div class="data-row"><span class="data-key">Hα / Hβ (obs)</span><span class="data-val flag">8.2</span></div>
      <div class="data-row"><span class="data-key">Hα / Hβ (expect)</span><span class="data-val ok">2.86</span></div>
      <div class="data-row"><span class="data-key">Hα / [NII]</span><span class="data-val warn">3.1</span></div>
      <div class="data-row"><span class="data-key">He II 4686</span><span class="data-val flag">Present</span></div>
      <div class="data-row"><span class="data-key">Balmer series</span><span class="data-val warn">Incomplete</span></div>
    </div>
    <div class="panel">
      <div class="panel-label">Source Context</div>
      <div class="data-row"><span class="data-key">Object type</span><span class="data-val warn">Unclassified</span></div>
      <div class="data-row"><span class="data-key">Temporal var.</span><span class="data-val flag">Yes (~6 days)</span></div>
      <div class="data-row"><span class="data-key">Blue abs. trough</span><span class="data-val flag">Present</span></div>
      <div class="data-row"><span class="data-key">X-ray assoc.</span><span class="data-val warn">Marginal</span></div>
      <div class="data-row"><span class="data-key">Radio flux</span><span class="data-val ok">Non-detect</span></div>
    </div>
  </div>

  <!-- OBSERVATION LOG -->
  <div class="section-title">Observation Log</div>
  <div class="panel">
    <div class="obs-entry"><span class="obs-ts">T+00:00</span><span class="obs-msg info">Spectrum acquired. Initial classification attempt: H II region.</span></div>
    <div class="obs-entry"><span class="obs-ts">T+00:14</span><span class="obs-msg alert">MISMATCH: Hα/Hβ ratio = 8.2. Expected ≤3.1 for standard recombination. Dust reddening alone insufficient to explain.</span></div>
    <div class="obs-entry"><span class="obs-ts">T+00:21</span><span class="obs-msg">Blue-shifted absorption trough detected at ~655.1nm. Consistent with outflowing material at ~500 km/s.</span></div>
    <div class="obs-entry"><span class="obs-ts">T+00:35</span><span class="obs-msg alert">He II 4686Å emission confirmed. Implies hard ionising photon source (T > 50,000 K or non-thermal).</span></div>
    <div class="obs-entry"><span class="obs-ts">T+00:48</span><span class="obs-msg">Temporal variability confirmed in archival cross-match. Period ~6.2 days. No radio detection.</span></div>
    <div class="obs-entry"><span class="obs-ts">T+01:02</span><span class="obs-msg info">Awaiting hypothesis selection. Standard Hα classification rejected.</span></div>
  </div>

  <!-- HYPOTHESES -->
  <div class="section-title">Candidate Hypotheses</div>

  <div class="hyp-card" onclick="selectHyp(this, 'A')" id="hyp-A">
    <div class="hyp-id">HYP-A</div>
    <div class="hyp-name">P Cygni Profile — Stellar Wind / Outflow</div>
    <div class="hyp-prob" id="prob-A">38%</div>
    <div class="hyp-desc">The blue absorption trough + redshifted broad emission is the classic P Cygni signature. A luminous hot star driving a fast stellar wind (LBV, WR, or nova ejecta) would produce absorption in the approaching wind and emission from surrounding expanding shell. The He II presence and variability are consistent.</div>
    <div class="prob-bar-wrap"><div class="prob-bar" style="width:38%"></div></div>
  </div>

  <div class="hyp-card" onclick="selectHyp(this, 'B')" id="hyp-B">
    <div class="hyp-id">HYP-B</div>
    <div class="hyp-name">Accretion Disk Emission — Compact Binary</div>
    <div class="hyp-prob" id="prob-B">28%</div>
    <div class="hyp-desc">Broad, asymmetric Hα emission is a hallmark of accretion disks in cataclysmic variables or X-ray binaries. Double-peaked profiles from Keplerian disk rotation can smear into a single broad feature. The 6-day variability could be the orbital period. He II 4686 is common in high-accretion-rate systems. Weak X-ray detection warrants further imaging.</div>
    <div class="prob-bar-wrap"><div class="prob-bar" style="width:28%"></div></div>
  </div>

  <div class="hyp-card" onclick="selectHyp(this, 'C')" id="hyp-C">
    <div class="hyp-id">HYP-C</div>
    <div class="hyp-name">Raman Scattering — Symbiotic Star System</div>
    <div class="hyp-prob" id="prob-C">19%</div>
    <div class="hyp-desc">In symbiotic binaries (red giant + white dwarf), OVI λ1032Å photons from the hot component can Raman-scatter off neutral hydrogen in the cool giant's envelope, producing broad emission near 6825Å and 7082Å. However, the same scattering mechanism operating on Lyman-beta can shift features near Hα. Anomalous Balmer ratios and the presence of He II favour this in symbiotic novae.</div>
    <div class="prob-bar-wrap"><div class="prob-bar" style="width:19%"></div></div>
  </div>

  <div class="hyp-card" onclick="selectHyp(this, 'D')" id="hyp-D">
    <div class="hyp-id">HYP-D</div>
    <div class="hyp-name">Shock-Excited Emission — Supernova Remnant Interaction</div>
    <div class="hyp-prob" id="prob-D">15%</div>
    <div class="hyp-desc">When a supernova blast wave hits dense circumstellar material, collisional excitation (not photoionisation) produces Balmer emission. Shock-excited Hα can be very broad (100s–1000s km/s) and the Balmer decrement departs dramatically from Case B. Narrow + broad component splitting and the lack of strong radio flux argues against a classical SNR but is plausible for a young remnant in dense CSM.</div>
    <div class="prob-bar-wrap"><div class="prob-bar" style="width:15%"></div></div>
  </div>

  <!-- INVESTIGATE -->
  <button class="investigate-btn" id="inv-btn" onclick="runInvestigation()" disabled>
    SELECT A HYPOTHESIS TO INVESTIGATE →
  </button>

  <!-- REASONING PANEL -->
  <div class="reasoning-panel" id="reasoning-panel">
    <div class="reasoning-header">
      <span>▸</span>
      <span id="reasoning-title">ANALYTICAL REASONING ENGINE — INITIALISING</span>
    </div>
    <div class="reasoning-body" id="reasoning-body">
      <div class="loading-dots"><span></span><span></span><span></span></div>
    </div>
  </div>

  <div class="verdict-block" id="verdict-block">
    <div class="verdict-title">OPERATOR VERDICT</div>
    <div id="verdict-content"></div>
  </div>

</div>

<script>
  let selectedHyp = null;

  function selectHyp(el, id) {
    document.querySelectorAll('.hyp-card').forEach(c => c.classList.remove('selected'));
    el.classList.add('selected');
    selectedHyp = id;
    const btn = document.getElementById('inv-btn');
    btn.disabled = false;
    btn.textContent = `INVESTIGATE HYP-${id} →`;

    // hide previous results
    document.getElementById('reasoning-panel').classList.remove('active');
    document.getElementById('verdict-block').classList.remove('active');
  }

  const systemPrompt = `You are SPECTRA-7, an astrophysical spectroscopy analysis engine. You provide rigorous, technically precise reasoning about unusual spectral observations. 

Your analysis style:
- Reference actual physics: quantum transitions, line formation mechanisms, diagnostic ratios
- Use real astrophysical quantities and name the key discriminating observations
- Be decisive but acknowledge what additional observations would confirm
- Write in flowing prose, not bullet points
- Keep it to ~200 words — dense and expert, not padded`;

  const hypData = {
    A: {
      title: 'P CYGNI PROFILE — STELLAR WIND ANALYSIS',
      prompt: `The observer has selected the P Cygni/stellar wind hypothesis to explain an anomalous emission at 656nm with: FWHM ~180 km/s (vs expected ~20 km/s), asymmetric profile, blue-shifted absorption trough at ~655.1nm (implying outflow at ~500 km/s), Hα/Hβ ratio of 8.2 (vs Case B 2.86), He II 4686Å detected, 6-day temporal variability, no radio detection.

Analyse whether the P Cygni hypothesis holds. Discuss the physics of line formation in stellar winds (Sobolev approximation, velocity law), what stellar class would produce these parameters, how He II fits in, and what the ~6 day variability could represent. What single follow-up observation would clinch or refute this interpretation?`
    },
    B: {
      title: 'ACCRETION DISK EMISSION — COMPACT BINARY ANALYSIS',
      prompt: `The observer has selected the compact binary/accretion disk hypothesis to explain an anomalous emission at 656nm with: FWHM ~180 km/s, asymmetric profile, blue-shifted absorption trough at ~655.1nm, Hα/Hβ ratio of 8.2 (vs Case B 2.86), He II 4686Å detected, 6-day temporal variability, marginal X-ray association, no radio detection.

Analyse whether the accretion disk hypothesis holds. Discuss the physics of disk emission line formation (double-peaked Keplerian profiles, disk wind, irradiation), what type of compact binary this could be (CV, LMXB, symbiotic), how the 6-day period fits an orbital scenario, why the Balmer decrement is elevated, and what the He II implies about the disk accretion rate. What single follow-up observation would confirm or refute this?`
    },
    C: {
      title: 'RAMAN SCATTERING — SYMBIOTIC SYSTEM ANALYSIS',
      prompt: `The observer has selected the Raman scattering/symbiotic star hypothesis to explain an anomalous emission at 656nm with: FWHM ~180 km/s, asymmetric and broad profile, blue-shifted absorption at ~655.1nm, Hα/Hβ ratio of 8.2, He II 4686Å present, 6-day variability, no radio detection.

Analyse whether the Raman scattering/symbiotic star hypothesis holds. Explain the Raman scattering mechanism (OVI photons scattering off HI in the giant's wind), what wavelength shifts this produces, why the anomalous Balmer decrement could arise from optical depth effects in a dense nebula, whether the blue absorption could be a disk wind from the accreting white dwarf, and how the He II arises in symbiotic novae. Is 6 days consistent with symbiotic binary orbital motion? What observation would distinguish this from hypothesis A?`
    },
    D: {
      title: 'SHOCK-EXCITED EMISSION — SNR INTERACTION ANALYSIS',
      prompt: `The observer has selected the shock-excited emission/supernova remnant hypothesis to explain an anomalous emission at 656nm with: FWHM ~180 km/s, broad asymmetric profile, blue-shifted absorption trough, Hα/Hβ ratio of 8.2 (vs Case B 2.86), He II 4686Å detected, 6-day temporal variability, no radio detection.

Analyse whether the shock-excited SNR/CSM interaction hypothesis holds. Explain shock-excited Balmer emission vs photoionised emission (the Chevalier & Fransson model), how non-radiative shocks produce narrow + broad Hα components, whether the observed width is consistent with shock velocities vs thermal broadening, why He II would appear in fast shock post-shock zones. How does the lack of radio emission constrain this? What does the 6-day variability imply — light travel time, CSM clumping? What would discriminate this from a compact binary?`
    }
  };

  async function runInvestigation() {
    if (!selectedHyp) return;

    const btn = document.getElementById('inv-btn');
    btn.disabled = true;

    const panel = document.getElementById('reasoning-panel');
    const body = document.getElementById('reasoning-body');
    const titleEl = document.getElementById('reasoning-title');
    const verdictBlock = document.getElementById('verdict-block');
    const verdictContent = document.getElementById('verdict-content');

    panel.classList.add('active');
    verdictBlock.classList.remove('active');
    titleEl.textContent = hypData[selectedHyp].title;
    body.innerHTML = '<div class="loading-dots"><span></span><span></span><span></span></div>';

    try {
      const response = await fetch("https://api.anthropic.com/v1/messages", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          model: "claude-sonnet-4-6",
          max_tokens: 1000,
          system: systemPrompt,
          messages: [{ role: "user", content: hypData[selectedHyp].prompt }]
        })
      });

      const data = await response.json();
      const text = data.content.map(b => b.text || '').join('');

      // Render with key terms highlighted
      const formatted = text
        .replace(/\b(Hα|Hβ|He II|P Cygni|Balmer|FWHM|Sobolev|Keplerian|Raman|Chevalier|km\/s|nm|Å)\b/g, '<span class="key-term">$1</span>')
        .replace(/\n\n/g, '</p><p style="margin-top:12px">')
        .replace(/\n/g, '<br>');

      body.innerHTML = `<p>${formatted}</p>`;

      // Generate verdict
      const verdict = await fetch("https://api.anthropic.com/v1/messages", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          model: "claude-sonnet-4-6",
          max_tokens: 1000,
          system: `You are a senior astrophysicist giving a brief operator verdict. Based on the hypothesis analysis, give a 2-3 sentence conclusion: state whether this hypothesis should be the primary classification, the single most discriminating follow-up observation needed, and what confidence level you assign. Be direct. No bullet points.`,
          messages: [
            { role: "user", content: hypData[selectedHyp].prompt },
            { role: "assistant", content: text },
            { role: "user", content: "Now give your operator verdict: primary classification status, the ONE follow-up observation that would be definitive, and your confidence level (0-100%)." }
          ]
        })
      });

      const vData = await verdict.json();
      const vText = vData.content.map(b => b.text || '').join('');
      verdictContent.textContent = vText;
      verdictBlock.classList.add('active');

    } catch (err) {
      body.innerHTML = `<span style="color:var(--h-alpha)">ANALYSIS ENGINE ERROR: ${err.message}</span>`;
    }

    btn.disabled = false;
    btn.textContent = `RE-INVESTIGATE HYP-${selectedHyp} →`;
  }
</script>
</body>
</html>
