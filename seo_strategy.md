# SEO Strategy

## In scope
- Public frontend routes in `artifacts/octagon` (root artifact)
- Public frontend routes in `artifacts/pathfinder` (`/pathfinder/`)
- Public frontend routes in `artifacts/spectra7` (`/spectra7/`)
- Static HTML shells, route discoverability, metadata, social sharing tags, AI crawler visibility, robots.txt, sitemap.xml, and related public assets

## Out of scope
- JSON API endpoints under `/api/**`
- Authenticated/admin-only areas if introduced later (none detected in the current scan)
- `artifacts/mockup-sandbox` and other development-only tooling artifacts

## Target audience
- Unknown — likely competition/demo evaluators and technical operators. Update when known.

## Primary keywords
- Unknown — update when known.

## Dismissed categories
- (None yet)
